const express = require("express");
// const {
//   generateTimetableRemoteForGroupMerge,
// } = require("./../../../controllers/GA/generate-remote-timetable");
const router = express.Router();

// router.post("/generate-remote-timetable", generateTimetableRemoteForGroupMerge);

module.exports = router;
